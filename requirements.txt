# NFC Bridge Server Dependencies
# ================================

# WebSocket server
websockets>=12.0

# Smart card / NFC reader support
# Note: pyscard requires swig to be installed on Windows
# If pip install fails, install via: pip install pyscard --only-binary :all:
# Or download wheel from: https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyscard
pyscard>=2.0.0

# Cryptography for BAC authentication (CCCD/ePassport)
pycryptodome>=3.19.0

# Windows service support
pywin32>=306; sys_platform == 'win32'

# Build tools (for creating executables)
pyinstaller>=6.0.0

# Optional: nfcpy for direct FeliCa access (Suica full read)
# Requires libusb - install Zadig driver for your reader
# nfcpy>=1.0.4
